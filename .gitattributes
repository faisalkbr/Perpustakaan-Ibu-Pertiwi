# Normalize line endings: LF in the repo, auto in working copy.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text
*.woff binary
*.woff2 binary

# Laravel/Windows tooling files keep their native endings.
*.bat text eol=crlf
